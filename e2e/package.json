{
  "name": "tile-lite-elite-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end UI tests (Playwright) for Tile Lite Elite. The only Node in an otherwise all-Rust repo, quarantined to this directory.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "clean": "../scripts/e2e-clean.sh"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}
